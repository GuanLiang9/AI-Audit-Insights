# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # required for file uploads (multipart/form-data)

# --- Config ---
pydantic-settings>=2.4

# --- PDF ingestion ---
pymupdf>=1.24                  # imported as "fitz"

# --- Embeddings + vector store ---
sentence-transformers>=3.0     # local embeddings (pulls in torch on first install)
chromadb>=0.5

# --- LLM provider (Gemini, free tier) ---
google-genai>=1.0           # current unified Google GenAI SDK
